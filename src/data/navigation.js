// The ordered client-journey steps and their navigation labels (route "keys",
// not URL paths — App.jsx maps each key to a /design/:step React Router route).
export const STEP_KEYS = ['type', 'level', 'style', 'materials', 'furniture', 'upload', 'summary', 'generate'];
